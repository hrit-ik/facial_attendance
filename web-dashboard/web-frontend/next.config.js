/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['i.ibb.co', 'static.wikia.nocookie.net', 'res.cloudinary.com'],
  },
}

module.exports = nextConfig
